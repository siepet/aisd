// Zadanie AL2.4
// Marcin Siepetowski

#include <stdio.h>
#include <math.h>

void heapify(int A[], int size, int length){
	size_t heapSize = size;
	int Length = length;
}
int build_heap(int A[], int size, int length){
	size_t heapSize = size;
	int Length = length

};
int main(int argc, char *argv[])
{
// argv[1] == input
// argv[2] == output
	FILE *input 	= fopen(argv[1], "r");
	FILE *output 	= fopen(argv[2], "w");

	return 0;
}
